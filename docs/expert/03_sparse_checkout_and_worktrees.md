# Sparse Checkout & Worktrees

## Sparse Checkout
Check out only parts of repository.

git sparse-checkout init
git sparse-checkout set <folder>

## Worktrees
Multiple working directories from one repo.

git worktree add ../new-folder branch-name